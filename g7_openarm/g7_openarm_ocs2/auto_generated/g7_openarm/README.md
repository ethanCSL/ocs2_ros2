Place the generated OCS2 helper libraries for `g7_openarm` here.

This directory is used by the `libFolder` launch argument and is separate from
the external PinnZoo dynamics shared library configured in `task_pinnzoo.info`.
